{% extends 'base.html.twig' %}
{% block body %}
    {% if clientSideValidation is same as ('false') %}
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {% else %}
        {{ form_start(form) }}
    {% endif %}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}